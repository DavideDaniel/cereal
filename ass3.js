var word = process.argv[2];
var w = 0;

	while (w < word.length) {
		var oo = word.charAt(w);
		if (oo === "o") {
			if (oo === "o") {
			}
			console.log("success");
		}
		else {
			console.log("failure");
		}
		w++;
}